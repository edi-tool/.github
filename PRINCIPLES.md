# edi-tool 開発原則

edi-tool のツールは、教育書の編集・学校現場で「あったらいいな」を小さく解くためのものです。
新しいツールを作るとき・既存ツールを直すときは、この原則に沿っているかを確認してください。

## 1. 使い始めるまで

1. **インストール不要**：ブラウザで URL を開けばすぐ使える。
2. **静的に公開できる**：GitHub Pages で配信できる構成を優先する。バックエンドやフレームワークは、必要性を README に書けるときだけ導入する。
3. **専門知識がなくても使える**：用語の説明・入力例・エラー時の次の一手を画面に出す。

## 2. データの扱い

4. **原稿・写真を端末外へ送らない**：解析はブラウザ内で完結させる。例外（利用者が明示的に押す「報告」ボタンなど）は、送信先と送信内容を README の「データの扱い」に書く。
5. **外部依存を明記する**：CDN から読み込むライブラリは、名前・バージョン・読み込み元を README に書き、バージョンを固定する（`scripts/check-static.mjs` で検査）。
6. **既存の URL・保存形式を壊さない**：公開 URL、クエリ、localStorage のキーと形式は互換性を保つ。変える場合は移行処理と CHANGELOG の記載をセットにする。

## 3. 判定・計算の誠実さ

7. **根拠と出典を明示する**：判定基準・計算式・データの出典（告示名・版・字数など）を README に書き、データの件数はテストで固定する。
8. **不確実性を隠さない**：自動判定には誤検知・見落としがあることを画面と README に書く。確度が低いときは「低い」と表示する。
9. **取得失敗を「該当なし」と表示しない**：ファイル読み込みやテキスト抽出・外部データ取得に失敗したら、失敗として表示する。0件と失敗を区別する。
10. **人が確認・修正できる余地を残す**：自動検出の結果は、利用者が見直し・手で直せる形で出す（例：黒板補正さんの四隅手動調整、漢字チェックの文脈表示）。

## 4. 画面

- iPhone Safari を基準に確認する（viewport、タップ領域、ファイル選択、canvas 面積上限）。
- 日本語 IME の変換中（`compositionstart`〜`compositionend`）に入力確定処理を走らせない。
- アクセシビリティを悪化させない：`<html lang="ja">`、画像の `alt`、ラベル付きフォーム、キーボード操作。

## 5. リポジトリの体裁

- README は `templates/README.template.md` の順序に揃える（一文で価値 → 公開 URL → 画面 → 使い方 → データの扱い → 根拠と出典 → 制限事項 → 開発 → 関連ツール → ライセンス）。
- 変更は `CHANGELOG.md`（Keep a Changelog 形式）に記録し、節目で GitHub Releases のタグを打つ。
- CI は `templates/workflows/ci-static.yml` を基本にする。テストは Node 標準の `node:test` を使い、npm 依存を増やさない。
- 開発用ドキュメント（`CLAUDE.md` / `AGENTS.md` / `DESIGN.md` / `progress.md` / `CHANGELOG.md` / `tests/` など）は `_config.yml` の `exclude` で公開ビルドから外す。
- 1 コミット 1 目的。生成物がある場合は原本と分け、CI で生成漏れを検出する。
- 秘密情報（API キー、トークン）をコードに書かない。
